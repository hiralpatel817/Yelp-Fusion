package {{PACKAGE}}.{{COMPONENT}}

import android.content.Context
import androidx.lifecycle.Transformations
import {{BASEPACKAGE}}.presentation.base.BaseMutableLiveData
import {{BASEPACKAGE}}.presentation.base.BaseViewModel
import {{BASEPACKAGE}}.extensions.distinct
import javax.inject.Inject


class {{VIEWMODEL}} @Inject constructor(
    context : Context
) : BaseViewModel() {

    private val _state = BaseMutableLiveData({{VIEWSTATE}}())
    val progressState = Transformations.map(_state) { state -> state.progressState }.distinct()
}
